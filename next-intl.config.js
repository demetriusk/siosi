// next-intl configuration for App Router
// This JS file is provided so the next-intl package can reliably import the config at runtime.
module.exports = {
  locales: ['en', 'es', 'ru', 'pt', 'fr', 'de', 'it', 'ua'],
  defaultLocale: 'en'
};
